kind: Deployment
apiVersion: apps/v1
metadata:
  name: dip-ethereum-client
  labels:
    app: dip-ethereum-client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dip-ethereum-client
  template:
    metadata:
      labels:
        app: dip-ethereum-client
    spec:
      containers:
      - name: dip-policy-storage
        image: <!--image-->
        ports:
        - containerPort: 3000
        env:
        - name: AMQP_MODE
          value: core
        - name: AMQP_HOST
          value: "$(RABBITMQ_SERVICE_HOST)"
        - name: AMQP_PORT
          value: "$(RABBITMQ_SERVICE_PORT_AMQP)"
        - name: AMQP_USERNAME
          valueFrom:
            secretKeyRef:
              name: rabbitmq-credentials
              key: AMQP_PLATFORM_USERNAME
        - name: AMQP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rabbitmq-credentials
              key: AMQP_PLATFORM_PASSWORD
