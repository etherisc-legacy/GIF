{
  "name": "@etherisc/dip_contracts",
  "version": "1.0.0",
  "scripts": {
    "dev": "truffle migrate --reset --compile-all --verbose-rpc --network ${NETWORK:-development}",
    "console": "truffle console --verbose-rpc"
  },
  "dependencies": {
    "truffle": "^4.1.14"
  }
}
