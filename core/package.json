{
  "name": "core-gif-contracts-deploy",
  "version": "1.0.0",
  "scripts": {
    "broadcast": "cp -a ./deploy/. ./gif-contracts && cd gif-contracts && node ./broadcast.js"
  },
  "dependencies": {
    "fs-extra": "^7.0.1"
  }
}
