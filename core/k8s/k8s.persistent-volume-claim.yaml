apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gif-contracts-claim
  labels:
    app: gif-contracts
    tier: infrastructure
spec:
  storageClassName: gif-contracts
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
